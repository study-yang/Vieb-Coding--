# 上线前检查清单（已自动化部分）

请在发布前逐项确认：

- [ ] 已在 CloudBase 控制台为所有云函数配置必要的环境变量（APPID/APPSECRET, SMTP_USER/SMTP_PASS 等）。
- [ ] 已在控制台测试 `getUserProfile.bindPhone` 成功返回手机号并迁移旧数据。
- [ ] CSV 导出已验证不会造成 Excel 公式注入（测试包含 `=CMD()` 等字符串）。
- [ ] 依赖已通过 `npm audit` 检查，没有高危漏洞或已制定修复计划。
- [ ] 云函数的日志没有输出敏感凭证（access_token、APPSECRET、SMTP_PASS 等）。
- [ ] 对外接口添加速率限制（AI 已有，反馈/绑定已新增限流）。
- [ ] 大量数据迁移路径已测试（迁移 1k/5k 条数据）。
- [ ] CI 已配置并在 PR 中阻止高风险依赖或 lint 错误（见 .github/workflows/ci.yml）。
- [ ] 隐私政策已更新并在用户界面提供相关链接（手机号处理说明）。

如需我逐项在代码中进行更严格保障（如对所有云函数添加统一限流、审计日志、或添加回滚机制），回复需要的具体项。